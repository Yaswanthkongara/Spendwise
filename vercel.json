{
  "version": 2,
  "installCommand": "if [ -d frontend ]; then cd frontend; fi && npm install",
  "buildCommand": "if [ -d frontend ]; then cd frontend; fi && CI=false npm run build",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
